<div class="row">
    <div class="col wide">{{type}}</div>
    <div class="col wide">{{qty}}</div>
    <div class="col wide">{{price}}</div>
    <div class="col">{{#getSubtotal}}{{/getSubtotal}}</div>
    {{#unless (isDetails)}}
    <div class="col right">
        <a href="#/deleteEntry/:{{_id}}">&#10006;</a>
    </div>
    {{/unless}}
</div>